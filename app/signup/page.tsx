'use client';import {useState} from 'react';import {createBrowserClient} from '@supabase/ssr';import {useRouter} from 'next/navigation';
export default function Signup(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState('');const r=useRouter();const go=async()=>{const s=createBrowserClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!);const {data,error}=await s.auth.signUp({email,password});if(error)setMsg(error.message);else {setMsg(data.session?'Account created.':'Check your email to confirm your account.');if(data.session)r.push('/dashboard')}};return <main className="auth"><div className="card"><b>RJA<span>•</span></b><h1>Build your workspace</h1><p>Free account first. Pro features activate after purchase.</p><input placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/>{msg&&<div className="notice">{msg}</div>}<button className="btn" onClick={go}>Create account</button><a href="/login">Already have an account?</a></div></main>}
