import {NextResponse} from 'next/server';
import {requirePro} from '../../../../lib/auth';
import {supabaseAdmin} from '../../../../lib/supabase';
import {ai,safeJson} from '../../../../lib/ai';
import {rate} from '../../../../lib/rate';
import {sameOrigin} from '../../../../lib/security';
export async function POST(req:Request){try{const u=await requirePro();if(!sameOrigin(req))return NextResponse.json({error:'INVALID_ORIGIN'},{status:403});if(!rate('match:'+u.id))return NextResponse.json({error:'RATE_LIMIT'},{status:429});const b=await req.json();const resume=String(b.resume||'').slice(0,30000);const job=String(b.job||'').slice(0,30000);if(!job||!resume)return NextResponse.json({error:'Job and resume evidence are required.'},{status:400});const raw=await ai('You are an evidence-first career analyst. Return ONLY valid JSON with keys score (0-100), verdict, strengths (array), gaps (array), actions (array), matched_requirements (array of {requirement,evidence,status}), truth_flags (array). Never invent candidate qualifications, employers, degrees, tools, metrics or outcomes. Distinguish explicit evidence from inference.','Analyze this job against this resume.\nJOB:\n'+job+'\nRESUME EVIDENCE:\n'+resume);const result=safeJson(raw);const {data:jobRow}=await supabaseAdmin().from('jobs').insert({user_id:u.id,url:b.url||null,title:b.title||null,company:b.company||null,description:job,match:result}).select().single();return NextResponse.json({...result,job_id:jobRow?.id||null})}catch(e:any){const status=e.message==='PRO_REQUIRED'?402:e.message==='UNAUTHENTICATED'?401:500;console.error('job_match_error',e); return NextResponse.json({error:e.message||'AI_MATCH_FAILED'},{status})}}

