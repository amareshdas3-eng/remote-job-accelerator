import {NextResponse} from 'next/server'; import {requireUser,hasEntitlement} from '../../../lib/auth'; import {supabaseAdmin} from '../../../lib/supabase'; import {jsonError,sameOrigin} from '../../../lib/security';
const statuses = ['saved', 'selected', 'ready_to_apply', 'applied', 'follow_up', 'screening', 'interview', 'offer', 'rejected', 'withdrawn', 'closed'];
export async function GET(){try{const u=await requireUser();const {data,error}=await supabaseAdmin().from('applications').select('*').eq('user_id',u.id).order('updated_at',{ascending:false});if(error)throw error;return NextResponse.json({applications:data||[]},{headers:{'Cache-Control':'no-store'}})}catch{return jsonError('UNAUTHENTICATED',401)}}
export async function POST(req:Request){try{const u=await requireUser();if(!await hasEntitlement(u.email||''))return jsonError('PRO_REQUIRED',402);if(!sameOrigin(req))return jsonError('Invalid origin',403);const b=await req.json();const status=statuses.includes(String(b.status))?String(b.status):'saved';const {data,error}=await supabaseAdmin().from('applications').insert({user_id:u.id,company:String(b.company||'').slice(0,200),role:String(b.role||'').slice(0,200),job_id:b.job_id||null,job_url:String(b.job_url||'').slice(0,1000),status,notes:String(b.notes||'').slice(0,5000)}).select().single();if(error)throw error;return NextResponse.json({application:data},{status:201})}catch(e){console.error(e);return jsonError('APPLICATION_CREATE_FAILED',500)}}
export async function PATCH(req:Request){try{const u=await requireUser();if(!await hasEntitlement(u.email||''))return jsonError('PRO_REQUIRED',402);if(!sameOrigin(req))return jsonError('Invalid origin',403);const b=await req.json();if(!b.id)return jsonError('Application id required');const patch:any={updated_at:new Date().toISOString()};for(const k of ['company','role','job_url','notes'])if(k in b)patch[k]=String(b[k]||'').slice(0,k==='notes'?5000:1000);if('status' in b&&statuses.includes(String(b.status)))patch.status=b.status;const {data,error}=await supabaseAdmin().from('applications').update(patch).eq('id',b.id).eq('user_id',u.id).select().single();if(error)throw error;return NextResponse.json({application:data})}catch(e){console.error(e);return jsonError('APPLICATION_UPDATE_FAILED',500)}}
