export async function track(event:string,distinct_id:string,properties:Record<string,unknown>={}){if(!process.env.POSTHOG_KEY)return;await fetch(`${process.env.NEXT_PUBLIC_POSTHOG_HOST||'https://us.i.posthog.com'}/capture/`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({api_key:process.env.POSTHOG_KEY,event,distinct_id,properties})}).catch(()=>{})}
